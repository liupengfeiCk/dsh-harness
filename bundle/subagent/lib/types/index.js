/**
 * dsh-harness-subagent-bundle — the Harness-owned subagent capability as
 * a profile bundle. The package's substance is `cordis.patch.yml`, declared by
 * the `dsh.bundle.patch` manifest field and resolved by the profile composer
 * through that field; the browser surface (independent "subagents" settings
 * section) is declared through `dsh.client` and rides the browser half.
 * @module dsh-harness-subagent-bundle
 */
/** Host plugin body — the registry, providers, and browser surface register through the patch rows. */
export function apply() { }
//# sourceMappingURL=index.js.map