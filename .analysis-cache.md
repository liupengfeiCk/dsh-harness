# Model-plan chip 菜单改造 — 分析与执行记录

日期：2026-08-20
任务：改造模型方案芯片菜单（composer 方案选择器）：①会话临时覆盖扩成任意参数 ②菜单视觉重修（修叠影 bug）。

## 现状侦查结论

- 临时覆盖当前**只有温度一个输入框**（ModelPlanChip.tsx:110-119 applyOverride），本地 `useState<string>`，保存 `select(planId, { temperature: Number })`。
- 覆盖值持久化为 `model-plan/select` **会话事件**（index.ts:311 `appendIgnorable`），UI store（mode-store.ts）的 `overrides` 字段读自 `readSelection`/`select` 返回值。
- **merge 层已支持任意 key**：merge.ts:95 `const bag = { ...plan.params, ...overrides }`，所有 key 进 `extra`（bagToExtra），优先级 `会话覆盖 > 方案 params > 官方 base`。plan.params 本就是任意袋（PlanParams = Record<string, JsonValue>）。
- **唯一限制在 UI 层**：applyOverride 只构造 temperature；且只能设一个值，不能任意 key/多行/string/object。
- wire 层（schema.ts wireParamsSchema、wire/index.ts toParams）已支持任意 overrides，host 无"仅温度"假设。
- **叠影 bug**：`.root` position:relative 无 z-index（未建层叠上下文），`.menu` z-index:50 在未建层叠上下文的父级内，无法盖住页面更高层元素（composer 底部统计栏）→ 菜单选中行与背后统计文字重叠。
- 视觉参照：方案弹窗参数区 ParamRow（section-store.ts ParamDraft / paramsToDraft / paramsToWire / isJsonValue），grid `1fr 1fr 28px`，`--dsw-alias-*` 变量。
- 构建：根 `npm run build` = tsc -b host + tsdown + tsc -b client + tsdown。测试：`pnpm exec vitest run`，thread-safe 池跑 `bundle/*/tests/**/*.spec.ts`，排除 `bundle/*/tests/ui/*.client.spec.*`（UI client.spec 不跑）。

## 关键工程发现（侦察阶段识别，纳入方案）

**换方案会清空覆盖，与验收③冲突**：当前 option onClick = `select(option.id)`（不带 overrides），会重置覆盖袋。验收③要求"换方案后覆盖仍在且优先级高于方案参数"，故换方案时必须把当前覆盖袋一起提交。

## 方案

改造一（任意参数覆盖）：
- mode-store.ts 新增 `overrideDraft: ParamDraft[]`（key/value 字符串）+ begin/set/add/remove/apply/clear 方法；JSON 解析复用 isJsonValue 语义。
- 换方案保留覆盖：`select` 未带 overrides 时补当前 `state.overrides`。
- chip 触发器加覆盖状态标记。

改造二（视觉）：
- `.root` 加 z-index 建层叠上下文修叠影；菜单分区（方案列表 / 覆盖区）；覆盖参数行小表格（对齐 section 参数区 grid，收敛尺寸）。

改动清单：
1. src/ui/mode-store.ts — 覆盖 draft 状态 + 方法 + 换方案保留覆盖
2. src/ui/ModelPlanChip.tsx — 覆盖区改任意参数行 + 触发器覆盖标记
3. src/ui/ModelPlanChip.module.css — 叠影修复 + 分区 + 小表格
4. src/ui/locales.ts — 新增覆盖区 locale key
5. tests/ui/modelplan-logic.client.spec.ts — 补 chip 覆盖 draft 测试
6. tests/merge.spec.ts — 补任意 key 覆盖透传/换方案优先级测试
7. merge.ts — **不改**（现状已支持任意 key）
